<%= form_with(model: [:admin, @till, till_session], class: "contents") do |form| %>
  <% if till_session.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(till_session.errors.count, "error") %> prohibited this till_session from being saved:</h2>

      <ul class="list-disc ml-6">
        <% till_session.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :opening_float %>
    <%= form.text_field :opening_float, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": till_session.errors[:opening_float].none?, "border-red-400 focus:outline-red-600": till_session.errors[:opening_float].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
